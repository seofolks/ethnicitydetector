streamlit==1.52.1
deepface==0.0.96
# TensorFlow 2.20.0 has Python 3.13 wheels (Cloud default).
tensorflow==2.20.0
tf-keras==2.20.0
# Headless OpenCV avoids libGL issues on Streamlit Cloud.
opencv-python-headless==4.10.0.84
Pillow>=10.4.0
# Numpy version will be determined by TensorFlow dependencies (likely >=2.1)
