streamlit==1.52.1
deepface==0.0.96
tensorflow==2.20.0
tf-keras==2.20.0
# Headless only — do not install opencv-python (needs broken libglib on Streamlit Cloud).
opencv-python-headless==4.10.0.84
Pillow>=10.4.0
numpy==2.2.1
