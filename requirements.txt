streamlit==1.52.1
deepface==0.0.96
# TensorFlow 2.20.0 has Python 3.13 wheels (Cloud default).
tensorflow==2.20.0
tf-keras==2.20.0
# Headless OpenCV avoids libGL issues on Streamlit Cloud.
opencv-python-headless==4.9.0.80
Pillow>=10.4.0
# Numpy >=2.1 is required by TF 2.20. Pin within compatible range.
numpy==2.2.1

