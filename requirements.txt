streamlit>=1.40.0
deepface==0.0.96
# TensorFlow 2.20.0 has Python 3.13 wheels (cloud default), so use it.
tensorflow==2.20.0
tf-keras==2.20.0
# Headless OpenCV avoids libGL issues on Streamlit Cloud.
opencv-python-headless==4.9.0.80
Pillow>=10.4.0
# Pin numpy to TF-compatible range.
numpy==1.26.4

